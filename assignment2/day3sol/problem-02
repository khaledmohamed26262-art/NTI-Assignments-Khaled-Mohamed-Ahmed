#include <stdio.h>

#define MAX_ARRAY_SIZE 100

int main(void)
{
    signed int arraySize = 0;
    signed int index = 0;
    signed long long int arraySum = 0LL;

    printf("Enter size of the array: ");

    if (scanf("%d", &arraySize) != 1 ||
        arraySize <= 0 ||
        arraySize > MAX_ARRAY_SIZE)
    {
        printf("Invalid array size!\n");
        return 1;
    }

    signed int arrayElements[arraySize];

    printf("Enter %d elements in the array:\n", arraySize);

    for (index = 0; index < arraySize; index++)
    {
        if (scanf("%d", &arrayElements[index]) != 1)
        {
            printf("Invalid array element!\n");
            return 1;
        }

        arraySum = arraySum + arrayElements[index];
    }

    printf("Sum of all elements of array = %lld\n", arraySum);

    return 0;
}